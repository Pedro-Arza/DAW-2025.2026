import { Component } from '@angular/core';

// Componente Footer: Es el pie de página que aparece abajo del todo
@Component({
  selector: 'app-footer',
  imports: [], // No necesita importar nada porque solo muestra texto estático
  templateUrl: './footer.html',
  styleUrl: './footer.css'
})
export class Footer {}
